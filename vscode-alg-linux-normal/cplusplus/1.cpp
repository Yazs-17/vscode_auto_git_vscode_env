#include <bits/stdc++.h>
#define ll long long
#define fer(i, m, n) for (ll i = m; i < n; i++)
using namespace std;
void solve() {
    int n;
    cin >> n;
    cout << n << endl;
    cout << 44 << endl;
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}